{
  "name": "bestcar",
  "version": "1.0.0",
  "description": "BestCar - Multi-tenant salvage car inventory and expense tracking system",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && bundle exec rails server -p 3000",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
